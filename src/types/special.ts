export type TerrorEvent = {
  title: string;
  time: string;
  position: string;
  city: string;
  minutes: string;
  isWidget?: boolean;
};
export type TerrorEventsNumbers = {
  today: number;
  week: number;
  month: number;
};
