# Copy to `.env` and adjust if needed.
# Only EXPO_PUBLIC_* values are inlined into the app bundle (treat as public).

# c14 API base URL.
EXPO_PUBLIC_API_BASE_URL=https://www.c14.co.il/wp-json/now14-api/v1
